# 天气数据获取详细规范

> 本文件从 SKILL.md 外置，包含 API 调用细节、天气代码转译表、温度跨度计算和多城市温差处理。
>
> **读取时机**：完成输入采集后、调用天气 API 前加载。

## API 调用方法

### 前置守卫（HARD GATE，进入本节前必做）

**判定 `approximate` 标志**（SKILL.md Step 2.3 采集阶段设置）：

- `approximate=true`（用户选"暂未确定"）→ **跳过本节全部 forecast/archive 调用**，直接走【L3：月度气候常态】。锚点日期仅用于 HTML 文案，绝不作为任何 API 的 `start_date`/`end_date`——forecast 窗口是 `today ~ today+16`，锚点（如 7 月 15 号）极易越界导致整段 400。
- `approximate=false` → 继续执行下方 Geocoding + 窗口判定 + Forecast 流程。

### 详细调用步骤（仅 `approximate=false` 生效）

对每个目的地城市：

1. **Geocoding**（获取经纬度）：
   ```
   https://geocoding-api.open-meteo.com/v1/search?name={城市名}&count=1
   ```
   - ⚠ **不要传 `language=zh` 参数**：实测中国城市（杭州/丽水/成都等）在 `language=zh` 下会返空 results，改为不带 language 参数即可正常返回。国际城市的中文名如"东京""巴黎"直接不带 language 也能命中。若首次失败可退化到英文名再查一次。
   - 若两次都失败，直接查已知坐标表（见下方 geocoding 失败回退章节）。

2. **窗口边界自动判定**（调用 Forecast 前必做）：
   - 计算 `today = 系统当前日期（用 Bash 跑 date +%Y-%m-%d 取准）`、`forecast_max = today + 16 天`
   - 若 `trip_end_date <= forecast_max` → 走 L1 forecast
   - 若 `trip_start_date > forecast_max` → **整段跳过 forecast，直接走 L2 archive-api 取去年同期**（forecast 会整段返错，不是部分裁剪）
   - 若 `trip_start_date <= forecast_max < trip_end_date`（跨窗口） → forecast 取 `start_date ~ forecast_max`，archive-api 取 `forecast_max+1 ~ trip_end_date` 的去年同期，两段拼接
   - HTML 头部对含 archive-api 数据的行程追加 ".approx-notice-history" 角标："部分日期使用去年同期数据估算"
   - **运行时兜底**：若 forecast 实际返回 `{"error":true, ...}` 或 HTTP 非 2xx（例如 API 端调整了窗口边界），**不要重试同一 URL**——立即降级到 L2 archive-api 去年同期，仍失败则降到 L3 气候常态。禁止靠改 `end_date` 一天天试探凑窗口，会耗掉整轮 token 预算。

3. **Forecast**（获取天气预报，start_date = 出发日期，end_date = 返回日期）：
   ```
   https://api.open-meteo.com/v1/forecast?latitude={lat}&longitude={lon}&daily=temperature_2m_max,temperature_2m_min,precipitation_sum,weathercode&timezone=auto&start_date={YYYY-MM-DD}&end_date={YYYY-MM-DD}
   ```
   - forecast API 允许范围严格是 `today ~ today+16`。end_date 超出 1 天也会整段 400，不做部分裁剪，须先按上面算法拆窗口。

4. **解析返回数据**，提取：每日最高温、最低温、降水量、天气代码（转译为中文描述）

5. **跨城温差检测**（多城市行程时必做）：
   - 计算各城市温度均值差 `|avg_A - avg_B|`
   - `≥ 8°C` → 触发"分城穿搭 + 洋葱式分层"，HTML 头部追加城市 chip 独立标签，按天穿搭强制按城市分段
   - 附加提醒："{A城} 均温 {avg_A}°C ↔ {B城} 均温 {avg_B}°C，温差 {Δ}°C，建议洋葱式分层携带"

## 回退方案（三级降级链，按优先级）

> **触发判定**：每次进入本节前先看 `approximate` 标志。`approximate=true`（用户在 Step 2.3 选择「暂未确定」）→ **直接跳到 L3**，不浪费 token 调用 L1/L2。`approximate=false` 但 forecast 失败 → 顺序 L1 → L2 → L3。

### L1：forecast API（首选）
- 适用：出发日期距今 ≤ 16 天，且 `approximate=false`
- 见上文「API 调用方法」第 2 步

### L2：archive-api（forecast 超期或失败时）
使用 Open-Meteo 历史天气 API 查询去年同期数据：
```
https://archive-api.open-meteo.com/v1/archive?latitude={lat}&longitude={lon}&daily=temperature_2m_max,temperature_2m_min,precipitation_sum,weathercode&timezone=auto&start_date={去年同日期}&end_date={去年同日期+天数}
```
返回数据格式与 forecast API 一致，可直接复用。

### L3：月度气候常态（最终兜底 / approximate=true 时直接走此路径）
- **取法**：根据锚点日期推出月份 → WebSearch 搜索"{城市} {月份} 平均气温 降水"或直接使用 Open-Meteo climate API（如 `https://climate-api.open-meteo.com/v1/climate?latitude={lat}&longitude={lon}&start_date=1991-01-01&end_date=2020-12-31&models=MRI_AGCM3_2_S&daily=temperature_2m_max,temperature_2m_min` 取月均值）→ 构造伪逐日数据（每天 max/min 都取月度均值，weathercode 取月度主导天气：6-8 月雨季偏 61，12-2 月寒带偏 71，其余取 1-3 多云）
- **HTML 头部强制标注**（approximate=true 或 L3 兜底时必须出现）：
  - 文案模板："📅 出发日未定，按 {目的地} {月份} 气候常态估算，建议出行前 3 天回看本清单"
  - 放在 `<header>` 标题正下方，class 标注 `.approx-notice`（黄底/橙字/小号），便于用户一眼识别"这是估算值"
- **按天穿搭模式**：approximate=true 时不渲染 Day 1/Day 2/… 逐日列表，改用「前半程穿搭」+「后半程穿搭」两段式（避免造假具体日期）
- **临走前待办**增加一条："出发前 3 天回看本清单，核对实时天气并补充/移除衣物"

### geocoding 失败回退（横向，独立于 L1/L2/L3）
geocoding API 返回空结果或 403 时，使用已知坐标表：
- 杭州: lat=30.25, lon=120.17
- 丽水: lat=28.47, lon=119.92
- 成都: lat=30.57, lon=104.07
- 康定: lat=30.05, lon=101.96
- 林芝: lat=29.65, lon=94.36
- 拉萨: lat=29.65, lon=91.17
- 大理: lat=25.58, lon=100.21
- 丽江: lat=26.87, lon=100.22
- 东京: lat=35.68, lon=139.69
- 巴黎: lat=48.86, lon=2.35
- 洛杉矶: lat=34.05, lon=-118.24
- 拉斯维加斯: lat=36.17, lon=-115.14
- 其他城市可通过 WebSearch 获取坐标

## 天气代码转译（合并为 4 档驱动穿搭）

按天穿搭只关心"晴/雨/雪/雾"四类，Open-Meteo 的九档合并如下：

| 代码 | 归档 | 中文描述 |
|------|------|---------|
| 0, 1-3 | 晴/多云 | 晴 或 多云（可细化文案） |
| 45, 48 | 雾 | 雾（追加"能见度低"note） |
| 51-55, 61-65, 80-82, 95-99 | 雨 | 雨（触发"带伞/雨衣"提示；≥ 95 附加"雷暴天气注意避险"） |
| 71-77 | 雪 | 雪（触发"防滑鞋 + 防水外套"） |

## 温度跨度计算

- 计算所有目的地、所有日期的温度范围（最低温 ~ 最高温）
- 温差 > 15°C：建议分层穿搭、可拆卸衣物
- 温差 ≤ 15°C：按平均温度推荐衣物

## 多城市行程温差处理

### 精确版（用户选择精确版或走文件解析分支时）

- 多城市行程时，按城市分段准备衣物：
  - 城市间温差 ≤ 10°C：按整体温度范围准备，无需分段
  - 城市间温差 > 10°C：按最冷城市准备基础保暖层 + 按各城市温度准备外层
  - 建议"洋葱式穿搭"：内层（透气排汗）+ 中层（保暖）+ 外层（防风防雨）
- 按天穿搭建议按城市分段显示，每个城市单独列出该城市期间的每日穿搭

### 快速版（用户在 2.1 选择快速版时）

- 对所有目的地城市统一查询天气，取行程期间所有城市温度的极值范围（全局最低温 ~ 全局最高温）
- 不按城市分段，按整体温度范围推荐衣物
- 按天穿搭区域改为「穿搭建议」简化版：一段文字描述温度范围 + 洋葱式分层建议 + 各城市间的温差提示（如有显著温差），不逐天列出
- HTML 中 `.daily-outfit` 模块仍渲染，但内容为 2-3 行概要建议而非 Day 1~Day N 逐行列表
